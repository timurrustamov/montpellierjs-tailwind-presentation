```html
<style>
  .card {
    background-color: rgb(31, 41, 55);
    border-radius: 4px;
    overflow: hidden;
    > img {
      width: 100%;
    }
    > div {
      padding: 1rem;
      > h2 {
        font-size: 1.25rem;
        color: rgba(255, 255, 255, 0.8);
      }
      > p {
        font-size: 1rem;
        color: rgba(255, 255, 255, 0.75);
      }
    }
  }
</style>

<div className="card">
  <img src="https://bit.ly/3cZ7Aqv" alt='"Semantic" CSS' />
  <div>
    <h2>"Semantic" CSS</h2>
    <p>
      We "separate our concerns" but there's a very obvious coupling between the CSS and HTML - most
      of the time the CSS is a mirror for the markup.
    </p>
  </div>
</div>
```
