```html
<style>
  .card {
    background-color: rgb(31, 41, 55);
    border-radius: 4px;
    overflow: hidden;
  }
  .card__image {
    width: 100%;
  }
  .card__content {
    padding: 1rem;
  }
  .card__title {
    font-size: 1.25rem;
    color: rgba(255, 255, 255, 0.8);
  }
  .card__body {
    font-size: 1rem;
    color: rgba(255, 255, 255, 0.75);
  }
</style>

<div className="card">
  <img src="https://bit.ly/3cZ7Aqv" alt="BEM CSS" />
  <div>
    <h2>Decoupling styles from structure</h2>
    <p>
      The markup is still "semantic" and do not contain any styling declisions, and the CSS feels
      decoupled from the markup structure with the added bonus of avoiding unnecessary selector
      specificity.
    </p>
  </div>
</div>
```
